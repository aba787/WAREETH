🎨 HTML and CSS code for statistics:
html
<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Warith Statistics</title>
<style>
@import url('https://fonts.googleapis.com/css2?family=Reem+Kufi:wght@400;600&family=El+Messiri:wght@400;600&family=Scheherazade+New:wght@400;600&family=Markazi+Text:wght@400;600&display=swap');

:root { 
--primary-brown: #8B4513; 
--golden: #DAA520; 
--dark-slate: #2F4F4F; 
--cream: #F5F5DC; 
--light-sand: #F0E68C; 
} 

body { 
background-color: var(--cream); 
font-family: 'Markazi Text', serif; 
margin: 0; 
padding: 20px; 
} 

.stats-section { 
text-align: center; 
padding: 60px 20px; 
background: linear-gradient(135deg, var(--primary-brown), var(--dark-slate)); 
colour: white; 
border-radius: 20px; 
margin: 40px 0; 
} 

.stats-section h2 { 
font-family: 'Reem Kufi', sans-serif; 
font-size: 2.8em; 
margin-bottom: 50px; 
color:var(--golden); 
} 

.stats-grid { 
display: grid; 
grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); 
gap: 40px; 
max-width: 1200px; 
margin: 0 auto; 
} 

.stat-card { 
background: rgba(255, 255, 255, 0.1); 
padding: 30px 20px; 
border-radius: 15px; 
backdrop-filter: blur(10px); 
border: 2px solid var(--golden); 
transition: transform 0.3s ease; 
} 

.stat-card:hover { 
transform: translateY(-10px); 
} 

.stat-number { 
font-family: 'El Messiri', sans-serif; 
font-size: 3.5em; 
font-weight: bold; 
color:var(--golden); 
margin-bottom: 15px; 
display:block; 
} 

.stat-label { 
font-family: 'Scheherazade New', serif; 
font-size: 1.4em; 
colour: white; 
line-height: 1.5; 
} 

.stat-icon { 
font-size: 2.5em; 
margin-bottom: 15px; 
display:block; 
}

/* Increasing count effect */
@keyframes countUp {
from {
opacity: 0;
transform: translateY(30px);
}
to {
opacity: 1;
transform: translateY(0);
}
}

.stat-number.animated {
animation: countUp 1.5s ease-out;
}
</style>
</head>
<body>
<section class="stats-section">
<h2>Warith Statistics</h2>
<div class="stats-grid">
<!-- Volunteer Hours -->
<div class="stat-card">
<span class="stat-icon">⏱️</span>
<span class="stat-number" data-target="3200">0</span>
<div class="stat-label">Total Volunteer Hours</div>
</div>

<!-- Number of Volunteers -->
<div class="stat-card">
<span class="stat-icon">👥</span>
<span class="stat-number" data-target="200">0</span>
<div class="stat-label">Number of Volunteers</div>
</div>

<!-- Number of Spaces -->
<div class="stat-card">
<span class="stat-icon">🏛️</span>
<span class="stat-number" data-target="600">0</span>
<div class="stat-label">Number of spaces</div>
</div>

<!-- Programs and Contributions -->
<div class="stat-card">
<span class="stat-icon">📊</span>
<span class="stat-number" data-target="8040">0</span>
<div class="stat-label">Programs and Contributions Beneficiaries</div>
</div>
</div>
</section>

<script>
// Incremental Count Function
function animateNumbers() {
const numberElements = document.querySelectorAll('.stat-number');

numberElements.forEach(element => {
const target = parseInt(element.getAttribute('data-target'));
const duration = 2000; // Count duration in milliseconds
const steps = 60; // Number of steps
const stepValue = target / steps;
let current = 0;

const timer = setInterval(() => {
current += stepValue;
if (current >= target) {
current = target;
clearInterval(timer);
}
element.textContent = Math.floor(current) + ' +';
}, duration / steps);

element.classList.add('animated');
});
}

// Run the count